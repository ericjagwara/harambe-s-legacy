# Use Montserrat throughout

## Changes
- Remove the Barlow Condensed font request.
- Set Montserrat as the global font for body text, headings, statistics, labels, buttons, and navigation.
- Preserve the existing hierarchy through Montserrat weights and current text sizes.
- Verify the homepage and Statistics page render with Montserrat and no font-related errors.

## Technical details
- Update the existing font link to load only the Montserrat weights already used by the site.
- Point both shared typography classes to Montserrat, so existing pages update consistently without changing their layouts.
